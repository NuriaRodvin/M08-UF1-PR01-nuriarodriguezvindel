import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm08-UF01-PR01-2-nuriarodriguezvindel',
  webDir: 'www'
};

export default config;
